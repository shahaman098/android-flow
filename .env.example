# Local only — copy to ~/Library/Application Support/voice-flow/.env
# Do not commit real keys. Prefer: bash cloud/deploy.sh

# MyGCP Cloud Run processing (default)
PROCESSING_MODE=cloud
FLOW_API_URL=https://flow-api-xxxxx-nw.a.run.app
FLOW_API_KEY=...

# Optional local-mode fallback (PROCESSING_MODE=local):
# DEEPSEEK_API_KEY=sk-...
# gcloud auth application-default login
