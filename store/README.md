# Play listing files

| File | Use |
|---|---|
| `icon-512.png` | Play Console high-res icon (Spout, the whale mascot) |
| `feature-graphic.png` | Play feature graphic 1024×500 |
| `app-release.aab` | Signed upload bundle (gitignored; copy of `android/app/build/outputs/bundle/release/app-release.aab`) |

Listing screenshots (Hub + bubble over Notes) live in `screenshots/`. Regenerated with `python3 store/gen_listing_shots.py`.
