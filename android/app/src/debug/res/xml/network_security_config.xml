<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug only: phone → local flow-api over LAN HTTP. Release uses HTTPS-only main config. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
